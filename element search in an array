#include<iostream>
using namespace std;
int main()
{
    int n,flag=0;
    cin>>n;
    int a[n];
    for(int i=1;i<=n;i++)
    {
        cin>>a[i];
    }
    int k;
    cin>>k;
    for(int i=0;i<n;i++)
    {
        if(a[i]==k)
        {
            flag++;
            break;
        }
        
    }
    if(flag==1)
    cout<<"True";
    else
    cout<<"False";
    
    
}
